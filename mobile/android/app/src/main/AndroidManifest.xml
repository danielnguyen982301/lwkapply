<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="LwkApply"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Lets the password-reset link emailed to the user
                 (https://lwkapply.vercel.app/reset-password?token=...,
                 built by backend/app/services/password_reset.py) open
                 straight into this app's ResetPasswordScreen instead of
                 only a browser - see features/auth/data/
                 deep_link_service.dart, which app_links hands this to.

                 android:autoVerify="true" makes this a real Android App
                 Link, verified against webapp/public/.well-known/
                 assetlinks.json (served at https://lwkapply.vercel.app/
                 .well-known/assetlinks.json). Without a verified App
                 Link, Android 12+ never offers this app as a handler
                 for an https:// link at all - no disambiguation dialog,
                 it just always opens in the browser (a pre-Android-12
                 behavior this comment used to assume still applied) -
                 and Gmail's own in-app link handling (Chrome Custom
                 Tabs) only ever hands off to a *verified* App Link
                 regardless of Android version, so this was silently
                 broken end-to-end until verification was added.

                 assetlinks.json's sha256_cert_fingerprints currently
                 points at this dev machine's auto-generated debug
                 keystore (~/.android/debug.keystore) - see
                 android/app/build.gradle.kts's release buildType, which
                 deliberately reuses the debug signingConfig since this
                 app has no separate release keystore (sideloaded APK
                 only, no Play Store listing). Regenerate that keystore
                 (or move to a real release keystore) and this stops
                 verifying until assetlinks.json's fingerprint is
                 updated to match - get the current one via `./gradlew
                 signingReport` from mobile/android. -->
            <intent-filter android:autoVerify="true" android:label="Reset password">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="https"
                    android:host="lwkapply.vercel.app"
                    android:pathPrefix="/reset-password"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
